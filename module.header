(function (factory) {
    if (typeof define === "function" && define.amd) {
        define(["react"], factory);
    } else if (typeof module !== "undefined" && module.exports) {
        module.exports = factory(require("react"));
    } else {
        window.InlineSVG = factory(window.React);
    }
})(function (React) {
